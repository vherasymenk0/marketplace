import { makeSxStyles } from '~/shared/helpers/makeSxStyles'

export const useStyles = makeSxStyles(() => ({
  container: {
    width: '100%',
    mt: { xs: 4, md: 3 },
  },
  submitBtn: {
    mt: 4,
  },
}))
